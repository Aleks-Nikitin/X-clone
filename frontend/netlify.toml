[build]
  base = "frontend/"
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/api/*"
  to = "https://x-clone-production-276a.up.railway.app"
  status = 200
  force = true

# SPA routing (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200